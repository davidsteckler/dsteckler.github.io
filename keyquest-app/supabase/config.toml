project_id = "keyquest"
[functions.keyquest-admin]
verify_jwt = false
# The function verifies every bearer token with Auth getUser, then checks kq_teachers.
# This supports current publishable keys without the legacy gateway JWT check.
